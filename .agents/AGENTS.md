# Project Rules

## Development Server Rules
- Always restart and ensure the backend server process (`cmd /c node src/index.js` in `server/`) is active and listening on `http://localhost:4000` after completing tasks or making edits.
