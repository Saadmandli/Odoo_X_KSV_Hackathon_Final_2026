# Copy to .env and fill in.
#
# Vite inlines VITE_ variables at build time, so this must be set in the host's
# environment before the production build runs. A bundle built without it will
# call localhost.
VITE_API_URL="http://localhost:4000/api"
